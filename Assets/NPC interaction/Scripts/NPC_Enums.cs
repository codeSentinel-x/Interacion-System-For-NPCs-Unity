using UnityEngine;

namespace NPC_Interaction.NPC_Enums {

    public enum NPC_Type {
        SimpleChatNPC,
        CustomChatNPC,
        ShopNPC,
    }
    public enum NPC_QuoteType {
        Random,
        InOrder,
    }
}
